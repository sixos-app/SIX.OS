#!/bin/bash
# First, let's inject our new CSS block at the end of styles.css to override old styles if needed, or we can replace existing ones.
# Actually, I will replace the existing CSS block for .mission-details-dialog in styles.css.
